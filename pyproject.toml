[project]
name = "my-game-list"
dynamic = ["version"]
description = "My Game List is a web application that allows users to manage their game lists."
readme = "README.md"
authors = [
    { name = "Rafał Grzegorczyk", email = "grzegorczyk1rafal@gmail.com" },
]
requires-python = ">=3.14"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3.14",
    "Framework :: Django",
]
dependencies = [
    "django==6.1",
    "djangorestframework==3.18.0",
    "django-filter==26.1",
    "django-extensions==4.1",
    "django-redis==7.0.0",
    "django-cors-headers==4.9.0",
    "PyJWT==2.13.0",
    "celery==5.6.3",
    "django-celery-results==2.6.0",
    "django-celery-beat",
    "gunicorn==26.0.0",
    "redis==8.1.0",
    "colorlog==6.12.0",
    "drf-spectacular==0.30.0",
    "pillow==12.3.0",
    "django_prometheus",
    "pip-prometheus==1.2.1",
    "prometheus-client==0.26.0",
    "psutil==7.2.2",
    "PyYAML==6.0.3",
    "setuptools==84.0.0",
    "requests-futures==1.0.2",
    "django_memcached_consul==0.2.0",
    "cryptography==50.0.0",
    "django-stubs-ext==6.1.0",
    "psycopg[binary]==3.3.4",
    "opentelemetry-api==1.44.0",
    "opentelemetry-sdk==1.44.0",
    "opentelemetry-exporter-otlp==1.44.0",
    "opentelemetry-instrumentation-django==0.65b0",
    "opentelemetry-instrumentation-celery==0.65b0",
    "opentelemetry-instrumentation-psycopg==0.65b0",
    "opentelemetry-instrumentation-redis==0.65b0",
    "opentelemetry-instrumentation-logging==0.65b0",
    "opentelemetry-instrumentation-requests>=0.65b0",
    "opentelemetry-instrumentation-urllib>=0.65b0",
    "opentelemetry-instrumentation-urllib3>=0.65b0",
    "django-modeltranslation>=0.20.3",
    "rapidfuzz==3.14.5",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = { attr = "my_game_list.__version__" }

[project.optional-dependencies]
test = [
    "pytest==9.1.1",
    "pytest-django==4.14.0",
    "freezegun==1.5.5",
    "pytest-icdiff==0.9",
    "pytest-xdist==3.8.0",
    "pytest-sugar==1.1.1",
    "model-bakery==1.24.0",
    "testcontainers[postgres]==4.15.0",
]
cov = ["pytest-cov==7.1.0"]
tox = ["tox-uv==1.36.0"]

[dependency-groups]
dev = [
    "isort==8.0.1",
    "ipython==9.16.1",
    "pre-commit==4.6.2",
    "black==26.5.1",
    "ruff==0.16.3",
    "mypy==2.3.1",
    "django-rosetta==0.10.3",
    "django-stubs==6.1.0",
    "djangorestframework-stubs==3.18.0",
    "types-setuptools==84.0.0.20260812",
    "types-requests==2.33.0.20260712",
    "types-psutil==7.2.2.20260518",
    "types-redis==4.6.0.20241004",
    "django-tui==24.5",
    "aiosmtpd==1.4.6",
    "django-filter-stubs==0.1.3",
    "typer==0.27.1",
    "celery-types==0.26.0",
]

[project.scripts]
django-admin = "django.core.management:execute_from_command_line"

[project.urls]
Homepage = "https://github.com/SojusanApps/my-game-list-backend"
Repository = "https://github.com/SojusanApps/my-game-list-backend"

[tool.uv.sources]
# Pinned to a commit on main, not a release: contains changes we need that
# haven't been cut into a PyPI release yet. Revisit once a release ships them.
django-celery-beat = { git = "https://github.com/celery/django-celery-beat", rev = "055e59071a730c35b58f6543a38048ecdc0ffba3" }
django-prometheus = { git = "https://github.com/django-commons/django-prometheus", rev = "ebfd4d6f8a5f92bbe503bd9dc93ddbfa9d8e43da" }

[tool.black]
# Compatible with the ruff line length
line-length = 120

[tool.ruff]
line-length = 120
extend-exclude = ["*/migrations/*", "stubs/*"]

[tool.ruff.lint]
# https://docs.astral.sh/ruff/rules/
select = [
    "F",     # Pyflakes
    "E",
    "W",     # pycodestyle
    "C90",   # mccabe
    "I",     # isort
    "N",     # pep8-naming
    "D",     # pydocstyle
    "UP",    # pyupgrade
    "ANN",   # flake8-annotations
    "ASYNC", # flake8-async
    "S",     # flake8-bandit
    "BLE",   # flake8-blind-except
    "FBT",   # flake8-boolean-trap
    "B",     # flake8-bugbear
    "A",     # flake8-builtins
    "COM",   # flake8-commas
    "C4",    # flake8-comprehensions
    "DTZ",   # flake8-datetimez
    "T10",   # flake8-debugger
    "DJ",    # flake8-django
    "EM",    # flake8-errmsg
    "EXE",   # flake8-executable
    "ISC",   # flake8-implicit-str-concat
    "ICN",   # flake8-import-conventions
    "LOG",   # flake8-logging
    "G",     # flake8-logging-format
    "INP",   # flake8-no-pep420
    "PIE",   # flake8-pie
    "T20",   # flake8-print
    "PYI",   # flake8-pyi
    "PT",    # flake8-pytest-style
    "Q",     # flake8-quotes
    "RSE",   # flake8-raise
    "RET",   # flake8-return
    "SLF",   # flake8-self
    "SLOT",  # flake8-slots
    "SIM",   # flake8-simplify
    "TID",   # flake8-tidy-imports
    "TC",    # flake8-type-checking
    "INT",   # flake8-gettext
    "ARG",   # flake8-unused-arguments
    "PTH",   # flake8-use-pathlib
    "TD",    # flake8-todos
    "ERA",   # eradicate
    "PGH",   # pygrep-hooks
    "PL",
    "PLC",
    "PLE",
    "PLR",
    "PLW",   # Pylint
    "TRY",   # tryceratops
    "FLY",   # flynt
    "PERF",  # Perflint
    "FURB",  # refurb
    "DOC",   # pydoclint
    "RUF",   # Ruff-specific-rules
]
ignore = [
    "D203", # in conflict with D211
    "D212", # in conflict with D213
]

[tool.ruff.lint.flake8-pytest-style]
mark-parentheses = true

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "PLR0913", "PLR0917"]
"scripts/*" = ["INP001"]
"docker/app/gunicorn.conf.py" = ["INP001"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
mypy_path = ["src", "stubs"]
plugins = ["mypy_django_plugin.main", "mypy_drf_plugin.main"]
strict = true

[tool.django-stubs]
django_settings_module = "my_game_list.settings.settings_mypy"

[[tool.mypy.overrides]]
module = "dacite"
implicit_reexport = true

[[tool.mypy.overrides]]
# modeltranslation replaces model managers with MultilingualManager at runtime;
# django-stubs cannot resolve the substituted manager type (django-manager-missing),
# and the resulting type-inference gap cascades into var-annotated errors on M2M
# fields that target translated models. Both codes are intentionally suppressed here.
module = "my_game_list.games.models"
disable_error_code = ["django-manager-missing", "var-annotated"]

[[tool.mypy.overrides]]
# Pinned to unreleased git commits (see [tool.uv.sources]); those builds don't
# ship a py.typed marker, so django-stubs can't analyze them for INSTALLED_APPS.
module = ["django_celery_beat.*", "django_celery_beat"]
ignore_missing_imports = true
